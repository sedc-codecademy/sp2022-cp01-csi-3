import React from 'react';
import PropTypes from 'prop-types';
import './UI/profile.css';

const UI/profile = () => (
  <div className="UI/profile">
    UI/profile Component
  </div>
);

UI/profile.propTypes = {};

UI/profile.defaultProps = {};

export default UI/profile;
